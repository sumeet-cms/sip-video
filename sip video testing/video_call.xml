<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  SIPp UAC scenario: video SIP call into a meeting.

  Flow:
    1. INVITE with audio (PCMU + RFC2833 telephone-event) and an H.264
       m=video line (sendrecv).
    2. On 200 OK, ACK and immediately start:
         - publishing ONE video stream  (play_pcap_video -> media/video.pcap)
         - a short audio stream          (play_pcap_audio -> media/audio.pcap)
    3. Wait 10 seconds.
    4. Send DTMF "560545#" (RFC2833) to join the meeting.
    5. Keep the call up for the recording window while we receive the
       mixed/composited video back from the server. (The inbound video is
       recorded out-of-band by tcpdump + tools/extract_received_video.py,
       see run_test.sh -- SIPp itself cannot write received RTP to disk.)
    6. BYE.

  This scenario assumes the trunk is IP-authenticated (no digest challenge).
  If the server replies 401/407, use video_call_auth.xml instead.

  Payload types:
    audio PCMU            = 0
    telephone-event/DTMF  = 96   (SIPp play_dtmf is hard-coded to PT 96)
    video H.264           = 97
  These MUST match the payload types baked into the generated pcaps
  (see tools/generate_media.sh).
-->

<scenario name="Video call + DTMF meeting join">

  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "sipp-video" <sip:[service]@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Allow: INVITE, ACK, CANCEL, BYE, INFO, OPTIONS, NOTIFY, REFER
      User-Agent: SIPp-Video-Test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp [call_number] [call_number] IN IP[local_ip_type] [media_ip]
      s=SIPp-Video-Test
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [auto_media_port] RTP/AVP 0 96
      a=rtpmap:0 PCMU/8000
      a=rtpmap:96 telephone-event/8000
      a=fmtp:96 0-15
      a=sendrecv
      m=video [auto_media_port+2] RTP/AVP 97
      a=rtpmap:97 H264/90000
      a=fmtp:97 profile-level-id=42e01f;packetization-mode=1;level-asymmetry-allowed=1
      a=rtcp-fb:97 nack pli
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true" />
  <recv response="180" optional="true" />
  <recv response="183" optional="true" />

  <!-- 200 OK. rrs="true" records the route set, rtd="true" stores the dialog. -->
  <recv response="200" rtd="true" rrs="true" />

  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "sipp-video" <sip:[service]@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      User-Agent: SIPp-Video-Test
      Content-Length: 0

    ]]>
  </send>

  <!-- IMPORTANT: SIPp has a single pcap-play engine per call, so the audio
       pcap, the DTMF, and the video pcap must run SEQUENTIALLY (never
       overlapping) or they corrupt each other. We therefore:
         audio keepalive (0-9s) -> wait to 10s -> DTMF join -> publish video. -->

  <!-- Keep the audio leg alive for the first ~9s (sent on the m=audio port). -->
  <nop>
    <action>
      <exec play_pcap_audio="media/audio.pcap" />
    </action>
  </nop>

  <!-- Wait 10 seconds after answer before sending the join DTMF. -->
  <pause milliseconds="10000" />

  <!-- Send DTMF "560545#" as RFC2833 to join the meeting. -->
  <nop>
    <action>
      <exec play_dtmf="560545#" />
    </action>
  </nop>

  <!-- Let the asynchronous DTMF finish (7 digits ~ 2.8s) BEFORE starting video. -->
  <pause milliseconds="4000" />

  <!-- Now publish a single video stream (sent on the m=video port, 6002).
       With symmetric RTP this also tells the server where to return the mixed
       video, which we record out-of-band via tcpdump (see run_test.sh). -->
  <nop>
    <action>
      <exec play_pcap_video="media/video.pcap" />
    </action>
  </nop>

  <!-- Recording window: stay in the meeting while we publish and receive the
       mixed video. Keep this shorter than the published video length
       (VIDEO_SECONDS, default 65s). -->
  <pause milliseconds="45000" />

  <send retrans="500">
    <![CDATA[

      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "sipp-video" <sip:[service]@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      User-Agent: SIPp-Video-Test
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true" />

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200" />
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000" />

</scenario>
