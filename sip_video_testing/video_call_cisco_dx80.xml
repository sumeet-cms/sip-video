<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  SIPp UAC scenario: Cisco DX-80 / Tandberg-style video call.

  Purpose
  -------
  Reproduces the key SDP characteristic of a Cisco DX-80 (CE firmware) INVITE:
  it offers TWO H.264 payload types in the same m=video line:

    PT 97  packetization-mode=0  (single-NAL)
    PT 126 packetization-mode=1  (non-interleaved)  ← preferred

  Both entries carry H.264 capacity parameters:
    max-br, max-mbps, max-fs, max-dpb, max-smbps

  The server MUST:
    1. Prefer PT 126 (explicit packetization-mode=1) over PT 97 (mode=0).
    2. Echo the capacity params for PT 126 back in the 200 OK fmtp line so
       that Cisco knows the bitrate/resolution limits.
       Without these params the Cisco endpoint shows a blank video tile.

  This scenario validates the fix in pkg/sip/video_sdp.go
  (selectH264 + addVideoAnswer).  After the call, run_test.sh checks that
  the SIPp message log contains the capacity params in the 200 OK answer.

  SIP headers and audio SDP are kept identical to video_call.xml so the
  call reaches the same dispatch rules and auth path that already work.

  Flow
  ----
    1. INVITE with dual-PT H264 SDP (PT 97 mode=0, PT 126 mode=1) + capacity params.
    2. 200 OK — server must select PT 126 and echo capacity params.
    3. ACK.
    4. Audio keepalive → wait 10 s → DTMF join → publish PT 126 video.
    5. Recording window (45 s).
    6. BYE.

  Payload types
  -------------
    audio PCMU           = 0
    telephone-event/DTMF = 96   (SIPp play_dtmf is hard-coded to PT 96)
    video H.264 mode-0   = 97
    video H.264 mode-1   = 126  ← what the server should select
  The video pcap (media/video_pt126.pcap) uses PT 126 — generated
  automatically by run_test.sh when this scenario is selected.
-->

<scenario name="Cisco DX-80 video call (dual-PT H264 + capacity params)">

  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "sipp-video" <sip:[service]@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Allow: INVITE, ACK, CANCEL, BYE, INFO, OPTIONS, NOTIFY, REFER
      User-Agent: SIPp-CiscoDX80-Test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=tandberg 15 1 IN IP[local_ip_type] [media_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [auto_media_port] RTP/AVP 0 96
      a=rtpmap:0 PCMU/8000
      a=rtpmap:96 telephone-event/8000
      a=fmtp:96 0-15
      a=sendrecv
      m=video [auto_media_port+2] RTP/AVP 97 126
      a=rtpmap:97 H264/90000
      a=fmtp:97 packetization-mode=0;profile-level-id=428014;max-br=2500;max-mbps=245000;max-fs=8160;max-dpb=16320;max-smbps=245000
      a=rtpmap:126 H264/90000
      a=fmtp:126 packetization-mode=1;profile-level-id=428014;max-br=2500;max-mbps=122400;max-fs=8160;max-dpb=16320;max-smbps=122400
      a=rtcp-fb:126 nack pli
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true" />
  <recv response="180" optional="true" />
  <recv response="183" optional="true" />

  <!-- 200 OK — the answer fmtp must select PT 126 and contain the capacity
       params. run_test.sh greps the message log to verify this. -->
  <recv response="200" rtd="true" rrs="true" />

  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "sipp-video" <sip:[service]@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      User-Agent: SIPp-CiscoDX80-Test
      Content-Length: 0

    ]]>
  </send>

  <!-- IMPORTANT: SIPp has a single pcap-play engine per call.
       audio keepalive (0-9 s) → wait 10 s → DTMF join → publish video. -->

  <!-- Keep the audio leg alive for the first ~9 s. -->
  <nop>
    <action>
      <exec play_pcap_audio="media/audio.pcap" />
    </action>
  </nop>

  <!-- Wait 10 s after answer before joining. -->
  <pause milliseconds="10000" />

  <!-- Send DTMF "560545#" (RFC2833, PT 96) to join the meeting. -->
  <nop>
    <action>
      <exec play_dtmf="560545#" />
    </action>
  </nop>

  <!-- Wait for DTMF to finish (~2.8 s + buffer). -->
  <pause milliseconds="4000" />

  <!-- Publish video at PT 126 (non-interleaved mode 1, matching what the
       server selects).  Symmetric RTP tells the server to return mixed video
       to this port, captured out-of-band by tcpdump. -->
  <nop>
    <action>
      <exec play_pcap_video="media/video_pt126.pcap" />
    </action>
  </nop>

  <!-- Recording window. Keep shorter than VIDEO_SECONDS (default 65 s). -->
  <pause milliseconds="45000" />

  <send retrans="500">
    <![CDATA[

      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "sipp-video" <sip:[service]@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: <sip:sipp@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      User-Agent: SIPp-CiscoDX80-Test
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true" />

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200" />
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000" />

</scenario>
